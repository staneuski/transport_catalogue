syntax = "proto3";

package transport.pb;

import "catalogue.proto";
import "map_renderer.proto";
import "graph.proto";

message DataBase {
    Catalogue catalogue = 1;
    renderer.Settings map_settings = 2;
    graph.pb.Graph graph = 3;
}